diff --git a/agent/agent_init.py b/agent/agent_init.py
index 8763af0..c08acbe 100644
--- a/agent/agent_init.py
+++ b/agent/agent_init.py
@@ -600,6 +600,7 @@ def init_agent(
     skip_context_files: bool = False,
     load_soul_identity: bool = False,
     skip_memory: bool = False,
+    skip_memory_snapshot_in_prompt: bool = False,
     skip_background_review: bool = False,
     session_db=None,
     parent_session_id: str = None,
@@ -700,6 +701,11 @@ def init_agent(
     # skip_memory=True already disables the memory-review trigger; this
     # flag is the explicit single-switch off for both review paths.
     agent.skip_background_review = bool(skip_background_review)
+    # skip_memory: skip external memory provider sync (store still loads when the
+    # memory toolset is explicitly enabled — see #65429).
+    # skip_memory_snapshot_in_prompt: keep the built-in store but omit MEMORY.md
+    # from the volatile system-prompt tier (reads/writes go through the tool).
+    agent._skip_memory_snapshot_in_prompt = bool(skip_memory_snapshot_in_prompt)
     agent.pass_session_id = pass_session_id
     agent.log_prefix_chars = log_prefix_chars
     agent.log_prefix = f"{log_prefix} " if log_prefix else ""
diff --git a/agent/system_prompt.py b/agent/system_prompt.py
index 90d6003..cd9bed9 100644
--- a/agent/system_prompt.py
+++ b/agent/system_prompt.py
@@ -828,7 +828,9 @@ def build_system_prompt_parts(agent: Any, system_message: Optional[str] = None)
         volatile_parts.append(skills_prompt)
 
     if agent._memory_store:
-        if agent._memory_enabled:
+        if agent._memory_enabled and not getattr(
+            agent, "_skip_memory_snapshot_in_prompt", False
+        ):
             mem_block = agent._memory_store.format_for_system_prompt("memory")
             if mem_block:
                 volatile_parts.append(mem_block)
diff --git a/gateway/run.py b/gateway/run.py
index 2fc8719..7917dce 100644
--- a/gateway/run.py
+++ b/gateway/run.py
@@ -5546,6 +5546,7 @@ class TurnRunner:
             user_id=getattr(ctx.source, "user_id", None),
             user_id_alt=getattr(ctx.source, "user_id_alt", None),
             skip_context_files=skip_context_files,
+            skip_memory_snapshot_in_prompt=True,
         )
         agent = None
         reused_cached_agent = False
@@ -5785,6 +5786,8 @@ class TurnRunner:
                 # Keep the persona even with minimal context: soul identity is
                 # a single small file, not part of the expensive walk.
                 load_soul_identity=True,
+                skip_memory=True,
+                skip_memory_snapshot_in_prompt=True,
             )
             if _cache_lock and _cache is not None:
                 with _cache_lock:
@@ -22792,6 +22795,8 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
                     session_db=getattr(self._session_db, "_db", self._session_db),
                     # Reload from disk — do not reuse the startup snapshot (#60955).
                     fallback_model=self._refresh_fallback_model(),
+                    skip_memory=True,
+                    skip_memory_snapshot_in_prompt=True,
                 )
                 try:
                     return agent.run_conversation(
@@ -26453,6 +26458,7 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
         user_id: str | None = None,
         user_id_alt: str | None = None,
         skip_context_files: bool = False,
+        skip_memory_snapshot_in_prompt: bool = False,
     ) -> str:
         """Compute a stable string key from agent config values.
 
@@ -26511,6 +26517,9 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
                 # (context files in vs out) — a toggled config edit must
                 # rebuild the cached agent, not silently reuse it.
                 bool(skip_context_files),
+                # skip_memory_snapshot_in_prompt changes whether MEMORY.md
+                # enters the volatile system-prompt tier — must bust cache.
+                bool(skip_memory_snapshot_in_prompt),
             ],
             sort_keys=True,
             default=str,
@@ -27308,6 +27317,17 @@ class GatewayRunner(GatewayAuthorizationMixin, GatewayKanbanWatchersMixin, Gatew
             if hasattr(agent, "_last_flushed_db_idx"):
                 agent._last_flushed_db_idx = 0
         agent._api_call_count = 0
+        # prosperfy-memory-snapshot-0784 — omit MEMORY.md from cached system prompts.
+        _want_skip_snapshot = True
+        _had_skip_snapshot = getattr(agent, "_skip_memory_snapshot_in_prompt", False)
+        agent._skip_memory_snapshot_in_prompt = _want_skip_snapshot
+        if (
+            _want_skip_snapshot
+            and not _had_skip_snapshot
+            and getattr(agent, "_cached_system_prompt", None)
+        ):
+            from agent.system_prompt import invalidate_system_prompt
+            invalidate_system_prompt(agent)
 
     def _commit_memory_before_soft_evict(self, agent: Any, key: str) -> None:
         """Fire on_session_end extraction before soft-evicting a live agent.
